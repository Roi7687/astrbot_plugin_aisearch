# 插件自身依赖清单
# 注意：AstrBot 核心已自带的包（aiohttp / fastapi / pydantic / certifi / httpx / Pillow 等）
# 请勿在此钉死精确版本，否则会触发 AstrBot 核心依赖版本保护（Core dependency version protection）。

# 浏览器自动化（反检测无头浏览器，依赖 playwright）
cloakbrowser==0.3.30
playwright==1.60.0

# HTML → Markdown 转换
markdownify==0.14.1

# 图片下载（AstrBot 核心自带 httpx，此处仅声明最低版本约束）
httpx>=0.28.1

# 图片压缩（可选；AstrBot 核心自带 pillow>=11.2.1）
Pillow>=11.2.1
